@model IEnumerable<CareerRoadmapGenerator.Models.UserProgress>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProgressPercentage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Career)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Skill)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProgressPercentage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Career.CareerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Skill.SkillId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserProgressId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserProgressId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserProgressId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
